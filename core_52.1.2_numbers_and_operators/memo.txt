ref 4. https://developer.mozilla.org/en-US/docs
      /Learn_web_development/Core/Scripting/Math
    In this article;
     ・Everybody loves math
     ・Arithmetic operators
     ・Increment and decrement operators
     ・Assignment operators
     ・Sizing a canvas box
     ・Comparison operators
     ・Summary
         
2026-09-17
folder: "core_52.1.2_numbers and operators", 

actions;
1. Preparaton
    copy folder from "core_52.1.1_Variables"
                to   "core_52.1.2_numbers and operators"
2. edit
2.35 index.html, style.css per ref 4. 
・Everybody loves math;
  We use different terms to describe 
 different types of decimal numbers, for example:
 -Integers
 -Floating point numbers (floats)
 -Binary
 -Octal
 -Hexadecimal

 ・It's all numbers to me;
   => ex-50.20 done!
   HTML;
    1. ex-50.9 incremental counterをcopy
     <button id="button_A">Press me</button>
     <h3 id="heading_A"></h3>
    2. <button id="button_A">Press me</button>を削除
   JS;
    3. const buttonA = document.querySelector("#button_A"); を削除
       const headingA = document.querySelector("#heading_A"); はkeep
    4. buttonA.onclick = () => {} を削除
    5. headingA.innerHTML = `myInt = ${myInt}`; を追記
  表示確認 ok! see: ex-50.20 numbers and operators-3, rendering ok!.png

　応用編:
// Gemini, 「値（5）」と「型（number）」を両方表示させたい場合
  JS;
  headingA.innerHTML = `myInt = ${myInt} (${typeof myInt})`;

// Gemini, 改行して表示したい場合
  JS;
  headingA.innerHTML = `値: ${myInt}<br>型: ${typeof myInt}`;

// Gemini, += を使って追記する場合（※上書きせずに後ろへ足す）
  JS;
  headingA.innerHTML = `myInt = ${myInt}`;
  headingA.innerHTML += ` (${typeof myInt})`; // += で追記

// 応用編 JK
  JS;
  headingA.innerHTML = `myFloat = ${myFloat}`;
  headingA.innerHTML += ` (${typeof myFloat})`; // += で追記
  
  JS;
  headingA.innerHTML = `myInt = ${myInt},
                    myFloat = ${myFloat}`;

 ・Useful Number methods;
   => ex-50.21 done!

 ・Converting to number data types,
    1. string to number, constructor Number()
   => ex-50.22 done!
  ref 4.1 https://developer.mozilla.org/en-US/docs/Web
        /JavaScript/Reference
        /Global_Objects/Number/Number 

=Notes=
文字列を数値型に型変換するには Number(), parseInt(), parseFloat()を使います。
Number()は簡単に使えて便利ですが、数字以外を含む文字列を引数に指定するとNaNを
返すので注意が必要です。

parseInt()は文字列を整数に変換し、実数の場合は小数点以下を切り捨てます。
また、第2引数で変換時の基数を指定することができ、省略した場合は10進数として
変換します。基数が10の場合は引数に数字以外の文字が含まれていても無視します。

parseFloat()は文字列を実数に変換します。引数に数字以外の文字が含まれている場合
は無視します。

Number('123');        // 123
Number('123a');       // NaN
parseInt('123', 10);  // 123（10進数の整数）
parseInt('ff', 16);   // 255（16進数の整数）
parseInt('2.8', 10);  // 2（小数点以下を切り捨てる）
parseInt('123a', 10); // 123（数字以外は無視）
parseFloat('2.8');    // 2.8（実数）
parseFloat('123a');   // 123（数字以外は無視）

=Notes=
  ref 4.2 https://developer.mozilla.org/ja/docs/Web
        /JavaScript/Reference
        /Global_Objects/Number/toString
      In this article;
       ・Try it
       ・Syntax
       ・Description
       ・Examples
  ・Try it     
   Using toString() method.    
   => ex-50.23 done!

 ・Increment and decrement operators,
    increment (++) and decrement (--) operators
    Assignment operators
   => ex-50.24 done! no rendering.

 ・Sizing a canvas box
  ref 4.3 https://developer.mozilla.org/en-US/docs
        /Web/API/Canvas_API
   => ex-50.25 done!

 ・Comparison operators
    Operator	Name
    ===       Strict equality     Tests whether the 
                                  left and right values are 
                                  identical to one another
    !==       Strict-non-equality
    <         Less than
    >         Greater than
    <=        Less than or equal to
    >=        Greater than or equal to

  quick example:
  => ex-50.26 done!




MDN ref 4. path;
Learn
Core learning modules
JavaScript
Numbers and operators


所見: 使える !!
